export function stripAnsi(text: string): string {
  return text;
}

export function wrapAnsi(text: string, _cols: number): string {
  return text;
}

export function visibleWidth(text: string): number {
  return text.length;
}
