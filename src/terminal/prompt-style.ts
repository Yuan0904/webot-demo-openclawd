export function formatPromptHeader(text: string): string {
  return text;
}

export function formatPromptChoice(text: string): string {
  return text;
}

export function formatPromptHint(text: string): string {
  return text;
}

export function stylePromptHint(text: string): string {
  return text;
}

export function stylePromptMessage(text: string): string {
  return text;
}

export function stylePromptTitle(text: string): string {
  return text;
}
