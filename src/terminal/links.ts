export function formatDocsLink(slug: string, _display?: string): string {
  return "https://docs.webbot.com/" + slug;
}

export function formatLink(text: string, _url?: string): string {
  return text;
}
