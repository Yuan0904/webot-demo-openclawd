/**
 * Stub module for wizard/clack-prompter.
 *
 * Re-exports the no-op prompter factory from prompts.ts so that
 * `import { createClackPrompter } from "../wizard/clack-prompter.js"` compiles.
 */

export { createClackPrompter } from "./prompts.js";
